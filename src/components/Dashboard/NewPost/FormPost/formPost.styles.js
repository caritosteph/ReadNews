const styles = theme => ({
  container: {
    display: 'flex',
    flexWrap: 'wrap',
    width: 400
  },
  formControl: {
    marginTop: 10,
    marginBottom: theme.spacing.unit,
    minWidth: 400,
  },
  button: {
    paddingTop: 20
  }
});

export default styles;
