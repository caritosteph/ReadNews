const styles = theme => ({
  container: {
    display: 'flex',
    flexWrap: 'wrap',
    width: 400
  },
  formControl: {
    marginTop: theme.spacing.unit,
    marginBottom: theme.spacing.unit,
    minWidth: 400,
  }
});

export default styles;
