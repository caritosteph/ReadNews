const styles = theme => ({
  gridCategories: {
    borderRight: `1px solid ${theme.palette.common.border}`
  },
  tabsCategories: {
    flexDirection: 'column'
  },
  categoryTitle: {
    height: 60,
    padding: 20
  }

});

export default styles;
